# Copy to .env.local before running the frontend.
#
# Where the FastAPI backend is listening. Must match the --port you pass to
# uvicorn. The repo defaults to 8010 because 8000 was already in use on the
# machine this was built on.
NEXT_PUBLIC_API_BASE=http://localhost:8010
